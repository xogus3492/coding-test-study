class Solution {
    public double solution(int[] arr) {
        double answer = 0;
        int a = arr.length;
            for(int i = 0; i<a; i++){
               answer +=  arr[i];
            }
        answer = answer / (double)a;
        return answer;
    }
}
