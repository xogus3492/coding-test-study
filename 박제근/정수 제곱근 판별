import java.math.*;

class Solution {
    public long solution(long n) {
        long answer = -1;
        long x = (long)Math.sqrt(n); // 제곱근 계산 함수
       
            if(x*x == n){
                answer = (x+1)*(x+1);          
            }
       
        return answer;
    }
}
